apply plugin: "war"

bootWar {
    mainClassName = "sat.gob.mx.agsc.ServiciosApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
